# Installing the ESP32 module

Insert the ESP32 module into the header sockets, ensure the USB-C socket is at the correct end, pointing downwards towards the text USB-C.

![Install ESP32](../img/rearcompleted.jpg)
